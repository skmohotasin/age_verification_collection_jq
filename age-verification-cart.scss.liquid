#modal_screen {
  position: fixed;
  top: 0;
  left: 0;
  height: 100%;
  width: 100%;
  z-index: 1100;
  background-color: rgba(0, 0, 0, 0.8);

  &.nope {
    background-color: #000;
  }
}

#modal_content {
  position: fixed;
  z-index: 1101;
  margin: 0 auto;
  background-color: {{ settings.age_verification_bg }};
  font-size: 12px;
  width: 290px;
  height: 365px;
  -webkit-box-sizing: border-box;
  -moz-box-sizing: border-box;
  -ms-box-sizing: border-box;
  -o-box-sizing: border-box;
  box-sizing: border-box;
  background-position: center 2em;
  background-repeat: no-repeat;
  background-size: 90px auto;
  .content_wrapper {
    margin: 0;
    position: absolute;
    top: 50%;
    width: 100%;
    -ms-transform: translateY(-50%);
    transform: translateY(-50%);
    text-align: center;
  }
  h2 {
    text-transform: none;
    color: {{ settings.age_verification_title_color }};
    margin: 0 0 0 0;
  }
  p {
    color: {{ settings.age_verification_body_color }};
    max-width: 95%;
    margin: 1.2em auto 0.9em;
    font: caption;
    a {
      color: {{ settings.age_verification_link_color }};
    }
    &.little {
      font-size: 0.8em;
    }
  }
  nav {
    font-size: 16px;
    display: table;
    width: calc(100% - 20px);
    margin: 0 10px;
    ul {
      display: table-row;
      width: auto;
      margin: 0;
      padding: 0;
    }
    li {
      display: block;
      text-align: center;
      width: auto;
      input {
        vertical-align: top;
        width: 22px;
        height: 22px;
      }
      label {
        padding-left: 10px;
    	color: {{ settings.age_verification_no_btn_color }};
    	vertical-align: text-bottom;
        font-weight: bold;
        line-height: 22px;
      }
    }
    &:after {
      content: "";
      display: block;
      clear: both;
    }
    small {
      display: block;
      text-align: center;
      color: #888;
      margin: 1em 0 0;
    }
    .av_btn {
      display: block;
      text-align: center;
      border-radius: 23px;
      margin: 1em auto 0;
      text-decoration: none;
      padding: 1em;
      color: #fff;
      text-transform: none;
      letter-spacing: 0;
    }
    .av_go {
      background-color: {{ settings.age_verification_yes_btn_color }};
      &:hover {
        background-color: {{ settings.age_verification_yes_btn_hover_color }};
      }
    }
    .av_no {
      background-color: {{ settings.age_verification_no_btn_color }};
      &:hover {
        background-color: {{ settings.age_verification_no_btn_hover_color }};
      }
    }
  }
  #modal_regret_wrapper nav a.av_btn {
    margin-top: 0;
    width: 230px;
    font-size: 0.8em;
  }
}

@media (min-width: 688px) {
  #modal_content {
    top: 10em;
    top: 30vh;
    left: 30vw;
    width: 90%;
    max-width: 500px;
    background-position: center 3em;
    background-size: 160px auto;
    font-size: 16px;
    h2 {
      margin: 0 0 0 0;
      font-size: 1.875em;
    }
    p {    
      font-size: 1em;
      line-height: 1.9em;
    }
    nav {
      display: table;
      width: calc(100% - 40px);
      margin: 0 20px;
      .av_btn {
        padding: 1em 2.5em;
        font-size: 1em;
      }
    }
  }
}
